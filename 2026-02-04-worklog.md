# 作業ログ — 2026-02-04

## 実施内容

### ■ MySQL 接続エラーの切り分け
発生事象：
- WebサーバからDBサーバへの接続でエラー発生
- ERROR 1044 / 1045（権限拒否）
- 接続元 `10.0.0.10` とユーザー権限の不一致を確認

対応：
- mysql に root でログインしてユーザー情報を確認
- 接続元ホストごとのユーザー設定を再確認
- 対象ユーザーの host 条件と権限を修正

確認SQL：
- `SELECT user, host FROM mysql.user WHERE user='vm';`
- `SHOW GRANTS FOR 'vm'@'10.0.0.10';`

---

### ■ MySQL 権限・ユーザー再設定
- GRANT 実行時にエラー発生（ユーザー未作成状態）
- CREATE USER → GRANT の順で再設定
- パスワードポリシー違反エラーが発生 → 条件を満たすパスワードへ変更

実施：
- ユーザー再作成
- 権限再付与
- FLUSH PRIVILEGES 実行

---

### ■ MySQL Tab補完トラブル対応
発生事象：
- mysql クライアントで Tab 補完時に異常動作
- コマンド途中で補完候補大量表示
- 誤入力により SQL 構文エラー発生

対応：
- Tab補完の仕様を確認
- `-A` オプションで高速起動モードがあることを確認
- show / use / table 指定の正しい入力手順を整理

---

### ■ mysqldump / バックアップ操作の再確認
- MySQL 内部コマンドと OS コマンドの違いを再確認
- dump 実行位置の整理
- バックアップ／リストア検証をポートフォリオ拡張候補に追加

---

## 発生したトラブルまとめ
- MySQL 接続権限エラー（1044 / 1045）
- GRANT 実行時のユーザー未作成エラー
- パスワードポリシー違反
- mysql Tab補完の誤動作による混乱
- Git push upstream 未設定エラー

---

## 学習ポイント
- MySQL は host 条件が一致しないと接続できない
- CREATE USER → GRANT の順序が必要
- mysql クライアントの補完は挙動を理解して使う

---

## 次回予定
- Web–DB 通信の再検証
- 監視サーバ（Zabbix）設定確認
