# 作業ログ — 2026-02-03

## 実施内容

### ■ ネットワーク構成の整理と確定
- 仮想環境のセグメント設計を整理
  - Host-Only：管理用（SSH）
  - Internal Network：Web–DB 間通信
  - DMZ（NAT）：外部公開用
- 各VMのNIC用途を明確化
- IPマップを更新・確定

---

### ■ Web / DB サーバ接続確認
- Web → DB 間の疎通確認を実施
- MySQL の待受設定を確認
- bind-address を `0.0.0.0` に設定して外部接続許可
- ポート3306の待受確認

確認コマンド：
- `ss -lntp | grep 3306`
- `grep bind-address /etc/mysql/mysql.conf.d/*`

---

### ■ MySQL 接続トラブル対応
発生事象：
- 外部ホストから MySQL 接続不可
- ERROR 2003 / 1044 / 1045 が発生

対応内容：
- mysql.user テーブルでユーザーと接続元ホストを確認
- 接続元IPごとのユーザー設定を確認
- GRANT を再設定
- パスワードポリシー制約によるユーザー作成エラーを修正

確認SQL：
- `SELECT user, host FROM mysql.user;`
- `SHOW GRANTS FOR 'vm'@'10.0.0.10';`

---

### ■ mysqldump 操作確認
- mysqldump を MySQL コンソール内で実行してエラー
- OSコマンドとして実行する必要があることを確認

誤：
mysql> mysqldump -u root -p dbname > backup.sql

正：
$ mysqldump -u root -p dbname > backup.sql

---

### ■ DB確認手順の整理
- テーブル未作成状態で SELECT 実行 → エラー発生
- 正しい確認手順を整理

手順：
1. `SHOW DATABASES;`
2. `USE dbname;`
3. `SHOW TABLES;`
4. `SELECT * FROM table;`

---

### ■ DBクライアント利用検討
- DBeaver での接続確認を検討
- GUIクライアントによるDB確認方針を決定

---

## 発生したトラブルまとめ
- MySQL 権限エラー
- 接続元ホスト制限による拒否
- パスワードポリシー制約エラー
- mysqldump 実行場所の誤り
- テーブル未作成参照エラー

---

## 学習ポイント
- MySQL は「ユーザー＋接続元ホスト」で認証される
- bind-address と Firewall の両方の設定が必要
- バックアップは OS コマンドで実行する
- 先にネットワーク設計を固めると構築が安定する

---

## 次回予定
- 監視サーバ構築の着手
- Zabbix 導入準備
- Web–DB 間通信制御の検証
